import React from 'react';

const AlertMessage = () => (
  <div>
    <sup>*</sup> Because of http source at api.openweathermap.org you could allow to load resources from unsecure places
    to let the WeatherApp work.
  </div>
)

export default AlertMessage;
