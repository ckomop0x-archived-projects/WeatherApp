import React from 'react';

export default function AlertMessage() {
	return <div>*Because of http source at api.openweathermap.org you could allow to load resources from unsecure places to let the WeatherApp work.</div>;
}
